#include<stdio.h>
void main()
{
	int num1=0,x,i,bin,len,base=1;
	printf("enter the binary number\n");
	scanf_s("%d",&bin);
	for(i=0;i<len;i++)
	{

		while(i<4)
		{
			a[i]=x*base;
			bin=bin/10;
			base=base*2;
		}	
	}
	printf("the converted decimal number is %d",num1);
}